@model ShoppingCartViewModel

@if (Model.ShoppingCart.ShoppingCartItems.Count > 0)
{ 
<li>
    <a>
        <span class="glyphicon glyphicon-shopping-cart"></span>
        <span id="cartstatus">@Model.ShoppingCart.ShoppingCartItems.Count</span>
    </a>

</li>
}